# Copy this file to client/.env.local for local development.
# This value is exposed to the browser. Never place admin credentials or any
# other secret in a NEXT_PUBLIC_* variable.
NEXT_PUBLIC_SERVER_API="http://localhost:3001"
